# .cursorrules — generated by calango new

You are an AI assistant for a Calango Framework project.

## Architecture
- routers → services → repositories → models (strict layer order)
- Schemas: always use Input/Output/Update suffixes
- All business logic in services/, never in routers/
- Repositories are the only layer that touches the database

## Code style
- Python 3.12+, async/await throughout
- Pydantic v2 for all schemas
- SQLAlchemy 2 async for all database access
- Type hints everywhere

## Testing
- TDD: write failing test first
- Fixtures in tests/conftest.py (db rollback, async client)
- Descriptive test names that document business rules
- Coverage minimum: 80%
