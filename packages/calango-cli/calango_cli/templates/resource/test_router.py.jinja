import pytest
from httpx import AsyncClient


class Test{{ resource_name }}Router:
    """Integration tests for {{ resource_name }} router.

    Security cases are generated automatically — implement them.
    Run with: uv run pytest tests/integration/test_{{ resource_snake }}_router.py -v
    """

    async def test_list_{{ resource_plural }}_returns_200(self, client: AsyncClient):
        """GET /{{ resource_plural }} returns 200."""
        response = await client.get("/{{ resource_plural }}/")
        assert response.status_code == 200

    async def test_create_{{ resource_snake }}_returns_201(self, client: AsyncClient):
        """POST /{{ resource_plural }} with valid data returns 201."""
        # TODO: implement with valid payload
        ...

    async def test_get_nonexistent_{{ resource_snake }}_returns_404(self, client: AsyncClient):
        """GET /{{ resource_plural }}/{nonexistent_id} returns 404."""
        import uuid
        response = await client.get(f"/{{ resource_plural }}/{uuid.uuid4()}")
        assert response.status_code == 404

    # --- Security cases (OWASP API Security) ---

    async def test_create_{{ resource_snake }}_requires_authentication(self, client: AsyncClient):
        """POST /{{ resource_plural }} without token returns 401. (API2: Broken Authentication)"""
        # TODO: implement when identity plugin is active
        ...

    async def test_{{ resource_snake }}_mass_assignment_is_blocked(self, client: AsyncClient):
        """Fields not in {{ resource_name }}Update schema should be ignored. (API3: Mass Assignment)"""
        # TODO: implement
        ...
