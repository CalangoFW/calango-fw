import uuid
import factory

from app.contexts.{{ context_snake }}.models.{{ resource_snake }} import {{ resource_name }}


class {{ resource_name }}Factory(factory.Factory):
    """factory-boy factory for {{ resource_name }} test fixtures.

    Usage:
        order = OrderFactory.build()          # in-memory, no db
        order = await OrderFactory.create()   # persisted
    """

    class Meta:
        model = {{ resource_name }}

    id = factory.LazyFunction(uuid.uuid4)
    # TODO: add field generators matching your model
    # name = factory.Sequence(lambda n: f"{{ resource_name }} {n}")
